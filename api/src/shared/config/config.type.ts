export type EnvConfig = {
    // 앱 기본 설정
    APP_PORT: string;

    // DB 설정
    DB_HOST: string;
    DB_PORT: string;
    DB_NAME: string;
    DB_USERNAME: string;
    DB_PASSWORD: string;
    DB_LOGGING: string;
}